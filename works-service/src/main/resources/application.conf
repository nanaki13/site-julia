
akka {
  # "akka.log-config-on-start" is extraordinarly useful because it log the complete
  # configuration at INFO level, including defaults and overrides, so it s worth
  # putting at the very top.
  #
  # Put the following in your conf/logback.xml file:
  #
  # <logger name="akka.actor" level="INFO" />
  #
  # And then uncomment this line to debug the configuration.
  #
  #log-config-on-start = true
}


# API prefix to be used for all API routes
apiPrefix = "api"


h2 = {
  url = "jdbc:h2:mem:test1"
  driver = org.h2.Driver
  connectionPool = disabled
  keepAliveConnection = true
}

postgres = {


  user="lkjtgmsdjbrwsw"
  driver = org.postgresql.Driver
  connectionPool = disabled
  keepAliveConnection = true
  url = "jdbc:postgresql://ec2-54-75-230-253.eu-west-1.compute.amazonaws.com:5432/d3nh1kkv05sci2"
  password = "0a69d761424f23404b57d55c05e2cab99be482b205f87888a6bcaea3c5bb05e4"
}